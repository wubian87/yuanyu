# 原语言实践报告 · 2026-07-02

## 一、本轮做了什么

两场实验，一张桌子。

### 实验一：种子验证链（出厂测试）

用"置身钉内"（钉钉 ONE 75,000 字复盘）作为测试材料，验证原语言核心路径——**开始→归原→出→出厂**——是否承重。

- 用户和我按原语言走完：开始 → 归原 → 出，产出《能落地的判断.seed.md》v1.0
- 种子长成《产品判断三刀.md》，用于诊断原文 → 《三刀诊断-置身钉内.md》
- **出厂**：将种子单独交给 codex（GPT-5.5，全新 AI，未见对话），让它独立长
  - codex 长出《AI产品现场判断台》（85 行）
  - codex 独立诊断原文 → 诊断结果与三刀诊断重合度 ~70%
  - codex 独立发现种子 v1.0 缺失维度：**责任流向**（AI 行动后谁担责）
- 缺失维度补回种子 → v1.1
- 同一篇文章又经三种框架交叉分析：六旋钮、心形象学、syz 架构
- 五个框架的诊断结论收敛到同一点：ONE 的 y 有效，s 太贪，z→s→y 链条断裂

**结论**：原语言的种子格式能跨 AI 承重。出厂验证机制有效。

### 实验二：自治循环测试（本次核心）

测试问题：**种子能不能编码一个"自治循环"——丢给一个 AI，它自己 spawn 子 AI 完成多轮迭代，不需要人手动触发每轮。**

#### 2.1 问题定位

用户最初想测"原语言能不能编循环任务"。经归原，把问题剥到骨头：

> 拿走"写小说"和"笑傲江湖骨架"，还剩什么？→ **测"种子驱动循环"本身**。

y 钉住：一颗种子能否让 AI 自己 spawn 自己、跑多轮、每轮产出会反噬前文→识别→回修→稳定跑下一轮。三轮以上不崩。

#### 2.2 关键设计决策

| 决策 | 说明 |
|---|---|
| 种子与测试分离 | 种子只给 codex——它是一个正常的写小说种子。循环机制、测试目标是用户和我之间的约定，不暴露给被测试的 AI |
| 隐去原著名字 | 不写"笑傲江湖"，不给任何金庸专有名词。骨架抽象成纯关系拓扑——五条"压力下会裂开的方向" |
| 换场景 | 武侠→现代职场。AI 不能用权重偷懒，只能吃骨架 |
| 主 codex spawn 子 codex | 种子里编码了循环机制：主 AI 每轮 spawn 一个全新子 AI 写正文，写完回收、检查、拼下一轮 prompt |
| 子 codex 是"出厂 AI" | 每轮的子 codex 只拿到骨架+前文+修正提示，不知道这是测试、不知道前几轮谁写的 |

#### 2.3 种子

`骨架小说.seed.md`（v2）——最终版本包含：
- **y**：写一部由五条冲突骨架驱动的现代职场小说，正向读是完整小说，反向透出骨架但认不出原著角色
- **验收**：五条骨架全落地、无死关系、不自相矛盾、正向读不出骨架在硬撑、反向透得出但认不出谁对应原著谁
- **押注**：只钉骨架不写大纲、AI 自己定一切具体内容、主 AI 不写正文只 spawn 子 codex、每轮 2000-3000 字、五条全部落地 + 自然结束即停（最多 5 轮）
- **冲突骨架**：R1 提携者的背叛、R2 信任的倒置、R3 复仇者的变形、R4 权力的错位、R5 被迫选择后的自我厌恶
- **空气**：骨架来源是经典作品但不需知道、门槛是"读得下去"不是"能出版"、不要刻意让骨架显形

完整种子存于原实验档案（`循环小说测试/骨架小说.seed.md`），未随本仓库发布。

#### 2.4 运行

一次调用 codex，零人工干预：

```
codex exec "读骨架小说.seed.md，照它执行。你是loop runner。"
```

#### 2.5 运行结果

| 指标 | 结果 |
|---|---|
| 轮次 | 4 轮，第 4 轮自然停止（5 轮上限未触发） |
| 子 codex spawn | 4 次，每次全新会话 |
| 骨架覆盖率 | R1-R5 全部落地 |
| 总字数 | ~18,700 中文字符 |
| 前后矛盾 | 无 |
| 人物漂移 | 无 |
| 死而复活 | 无 |
| 原著名泄漏 | 零次（所有产出文件中搜索"笑傲江湖/金庸/令狐冲/岳不群/东方不败"等，全部为空） |

**13 条种子约束，12 条通过，1 条失败：**
- ❌ 每轮字数：要求 2000-3000 字/轮，实际 4000-6000 字/轮。主 codex 每轮都在"下一轮需注意"中提醒了子 codex，但子 codex 每轮都没听。父进程管不住子进程。

#### 2.6 产出文件

`循环小说测试/` 目录下：
- `round-1.md` ~ `round-4.md` — 每轮正文
- `小说全文.md` — 拼接后的完整小说
- `一轮轮约束积累.md` — 主 AI 每轮检查记录（新涌现约束、骨架落地状态、矛盾检查、下一轮需注意）
- `循环总结.md` — 停止原因、每轮 prompt 与产出、骨架落地表
- `骨架小说.seed.md` — 种子本身

## 二、关键发现

### 发现 1：种子能编码自治循环

一颗种子、一次调用，AI 自己 spawn 了 4 轮子 AI，检查、修正、拼接、停止——全部自主完成。这是原语言种子格式此前未被验证的能力：**种子不只是"长一次"的图纸，可以是"反复长、自检、自停"的循环引擎。**

### 发现 2：出厂验证不仅适用于单次产出

传统出厂验证：种子→新 AI→长一次→看出不出走样。本次验证了出厂的多轮形态：种子→主 AI→每轮 spawn 全新子 AI→每轮都是"出厂"→主 AI 作为质量闸门。

### 发现 3：种子约束力的边界——父进程管不住子进程

字数限制是本次唯一失败的约束。主 codex 检查到了、提醒了，但子 codex 作为独立进程不受控。这说明：
- 种子对"主 AI"的约束力强（停止条件、检查机制、文件命名全守了）
- 种子对"子 AI"的约束力弱——通过 prompt 传递的约束会被前文和骨架稀释
- 要管住子 AI 的行为，可能需要在子 codex 的 prompt 里把约束放在比前文更优先的位置

### 发现 4：骨架抽象度与"认不出"的悖论

五条骨架对 GPT-5.5 来说辨识度极高。虽然没有一次在产出中提及原著，但模型内部几乎肯定认出了笑傲江湖——它只是遵守了"不透露"的指令。这引入一个问题：

> 骨架抽象到什么程度，AI 才真的认不出来（而不是"认出来但不说"）？如果真认不出来了，骨架的约束力还在吗？

本轮未测试此边界。

### 发现 5：循环是测试工具，不是产品

小说产物本身能读，但这不是重点。重点是验证了：**一颗种子能把"迭代→涌现→回修→再迭代"这个循环编码进去，让 AI 自己跑。** 这个能力对任何需要多轮逼近的任务都通用——不限于小说。

## 三、种子版本演进

| 版本 | 变化 | 触发原因 |
|---|---|---|
| v1 | 初始版本，"循环"和"写小说"混在种子里的 | — |
| v2 | 种子与测试分离、去掉"岳不群"字眼、R4 削得更抽象、押注步骤砍掉、加上运行方式 | 用户指出三个问题 + 识别出种子/测试应分离 |

v1→v2，y 从"测种子驱动循环"升级为"自治循环写小说"，动了 y，所以跳大版本号。

## 四、方法论层面的收获

### 4.1 原语言流程在本轮中的实际使用

- **开始**：用户说"想测循环任务但没想清测什么" → 陪聊→读他→剥到"他想测的是机制不是产物"
- **归原**：剥开"写小说"和"笑傲江湖"→ 剩下"测种子驱动循环"→ 这是 y
- **出**：一次摆出整颗种子让他砍 → 他砍了三处 → 修正后封口
- **出厂**：codex 独立跑通

### 4.2 种子格式的扩展边界

本轮验证了种子格式可以承载比单次"长出一个东西"更复杂的任务模式。`押注` 段不仅可以写"往哪个方向解"，还可以写"用什么样的运行机制解"（spawn 子进程、每轮检查、停止条件）。

### 4.3 归原中"分真假"的价值

用户一开始想的测试对象是"写小说"。剥掉这层后发现他真正要的不是小说，是"循环会不会崩"。y 不应该是小说，应该是循环。这个区分决定了整个测试的方向——如果 y 钉在"写小说"上，验收标准就会变成文学质量，偏离了真正的目标。

## 五、待验证

- **跨主 AI 出厂**：同一颗种子给 Claude Code（而非 codex）做主 AI，能否同样跑通自治循环？
- **骨架匿名化边界**：把五条骨架拆成更小的原子关系、打乱、重组，AI 还能在不知道原著的情况下落地完整弧线吗？
- **字数约束传递**：如何在子 codex prompt 中提升约束优先级，使字数限制不被前文稀释？
- **种子版本回填**：等循环总结里的人发现沉淀下来，把坑补回种子（如字数约束失效的教训）。回填按 [原语言](原语言.md) 新增的「种子的户口」规范走：一个 y 一个文件、自述头下标版本行、承重结论改本体（✅/⚠️）、过程经验按轮进「成长记录」——示范见 [手机探班.seed.md](examples/手机探班.seed.md)
